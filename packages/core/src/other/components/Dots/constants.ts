import rem from '../../../styles/spaces/rem';

export const SPAN_SIZE = 3;

export const SIZE_LARGE = rem(8);
export const SIZE_MEDIUM = rem(6);
export const SIZE_SMALL = rem(4);

export const WIDTH_SMALL =
  (rem(8) + rem(5) * 2) * 3 + (rem(6) + rem(5) * 2) + (rem(4) + rem(5) * 2);

export const WIDTH_MEDIUM = WIDTH_SMALL + (rem(6) + rem(5) * 2);

export const WIDTH_LARGE = WIDTH_MEDIUM + (rem(4) + rem(5) * 2);
