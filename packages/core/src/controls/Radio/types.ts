import {ViewStyle} from 'react-native';

import {ICommonControlProps} from '../types';

export interface IRadioStyle extends ICommonControlProps {
  outerStyle?: ViewStyle;
  innerStyle?: ViewStyle;
}
